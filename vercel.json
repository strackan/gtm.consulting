{
  "buildCommand": "cd adventure && npm install && npm run build && cp dist/index.html . && cp -r dist/assets .",
  "outputDirectory": ".",
  "framework": null,
  "rewrites": [
    {
      "source": "/adventure/:slug",
      "destination": "/adventure/index.html"
    }
  ]
}
